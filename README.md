# ai-opportunity-compass

Understand how you can create value in the AI era.

## Project setup

This repository now contains a Next.js (App Router) project skeleton using JavaScript and Tailwind CSS.

To get started locally:

1. Install dependencies:

   npm install

2. Run the development server:

   npm run dev

Notes:
- No pages have been created yet; the app/router layout is in `app/layout.js` and global styles in `styles/globals.css`.
- Tailwind is configured via `tailwind.config.js` and `postcss.config.js`.
